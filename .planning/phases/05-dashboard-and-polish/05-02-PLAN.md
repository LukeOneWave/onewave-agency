---
phase: 05-dashboard-and-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/app/page.tsx
  - src/components/dashboard/StatCards.tsx
  - src/components/dashboard/ActivityFeed.tsx
  - src/components/dashboard/UtilizationChart.tsx
autonomous: false
requirements:
  - DASH-01
  - DASH-02
  - DASH-03

must_haves:
  truths:
    - "Dashboard displays stat cards showing active sessions, agents used, and tokens consumed"
    - "Dashboard shows a chronological activity feed with typed events"
    - "Dashboard shows a horizontal bar chart of agent utilization with agent colors"
    - "Empty states render meaningful messages when no data exists"
  artifacts:
    - path: "src/components/dashboard/StatCards.tsx"
      provides: "Three stat cards in a responsive grid"
      min_lines: 20
    - path: "src/components/dashboard/ActivityFeed.tsx"
      provides: "Scrollable chronological event list"
      min_lines: 25
    - path: "src/components/dashboard/UtilizationChart.tsx"
      provides: "Recharts horizontal bar chart (client component)"
      contains: "use client"
    - path: "src/app/page.tsx"
      provides: "Server component orchestrating dashboard data and layout"
      contains: "dashboardService"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/lib/services/dashboard.ts"
      via: "import dashboardService, call getStats/getRecentActivity/getAgentUtilization"
      pattern: "dashboardService\\.get"
    - from: "src/components/dashboard/UtilizationChart.tsx"
      to: "recharts"
      via: "import BarChart, Bar, XAxis, YAxis, Tooltip, ResponsiveContainer"
      pattern: "from \"recharts\""
---

<objective>
Install Recharts, build dashboard UI components, and rewrite the dashboard page to display stats, activity feed, and utilization chart.

Purpose: Transforms the placeholder dashboard into a full-featured command center showing aggregate stats (DASH-01), activity timeline (DASH-02), and agent utilization visualization (DASH-03).
Output: Production-ready dashboard with three sections powered by the dashboardService from Plan 01.
</objective>

<execution_context>
@/Users/luke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard-and-polish/05-RESEARCH.md
@.planning/phases/05-dashboard-and-polish/05-01-SUMMARY.md

@src/app/page.tsx
@src/lib/services/dashboard.ts
</context>

<interfaces>
<!-- From Plan 01 dashboard service (will exist after 05-01 completes) -->
```typescript
// src/lib/services/dashboard.ts
export const dashboardService = {
  getStats(): Promise<{ activeSessions: number; agentsUsed: number; tokensConsumed: number }>,
  getRecentActivity(limit?: number): Promise<ActivityItem[]>,
  getAgentUtilization(): Promise<AgentUtilization[]>,
};

type ActivityItem = {
  type: "chat" | "mission" | "deliverable";
  description: string;
  timestamp: string; // ISO string
};

type AgentUtilization = {
  name: string;
  color: string;
  sessions: number;
};
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install Recharts and build dashboard components</name>
  <files>
    src/components/dashboard/StatCards.tsx
    src/components/dashboard/ActivityFeed.tsx
    src/components/dashboard/UtilizationChart.tsx
  </files>
  <action>
    Install recharts: `npm install recharts`

    Create three components in src/components/dashboard/:

    **StatCards.tsx** (server component, no "use client"):
    - Props: `{ stats: { activeSessions: number; agentsUsed: number; tokensConsumed: number } }`
    - Render 3 cards in a `grid grid-cols-1 md:grid-cols-3 gap-4` layout
    - Card 1: Activity icon + activeSessions count + "Active Sessions" label
    - Card 2: Users icon + agentsUsed count + "Agents Used" label
    - Card 3: Zap icon + tokensConsumed (formatted with `Intl.NumberFormat("en", { notation: "compact" })`) + "Tokens Consumed" label
    - Use existing card styling pattern: `flex items-center gap-4 rounded-lg border p-6`
    - Import icons from lucide-react: Activity, Users, Zap

    **ActivityFeed.tsx** (server component, no "use client"):
    - Props: `{ activities: ActivityItem[] }` where ActivityItem has type, description, timestamp
    - Render inside a `rounded-lg border p-6` container with "Recent Activity" heading
    - Each item shows: icon by type (MessageSquare for chat, Rocket for mission, CheckCircle for deliverable), description text, relative timestamp
    - Use simple relative time helper: compute "Xm ago", "Xh ago", "Xd ago" from ISO timestamp
    - Empty state: "No activity yet. Start chatting with agents to see activity here."
    - Show scrollable list with max-h and overflow-y-auto for long feeds

    **UtilizationChart.tsx** (client component, "use client"):
    - Props: `{ data: { name: string; color: string; sessions: number }[] }`
    - Empty state: show message "No agent usage data yet. Start chatting to see utilization."
    - Non-empty: Recharts horizontal BarChart inside ResponsiveContainer (width 100%, height 300)
    - Layout="vertical", XAxis type="number", YAxis type="category" dataKey="name"
    - Bar dataKey="sessions" with Cell components using each entry's color
    - Tooltip enabled for hover details
    - Wrap in rounded-lg border p-6 with "Agent Utilization" heading
    - Margin left 80px for agent name labels
  </action>
  <verify>
    <automated>cd /Users/luke/onewave-agency && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Three dashboard components exist, recharts installed, build succeeds without errors</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite dashboard page with service integration</name>
  <files>src/app/page.tsx</files>
  <action>
    Rewrite src/app/page.tsx as a server component that:

    1. Imports dashboardService from "@/lib/services/dashboard"
    2. Imports StatCards, ActivityFeed, UtilizationChart from "@/components/dashboard/"
    3. Fetches all data in parallel: `const [stats, activities, utilization] = await Promise.all([dashboardService.getStats(), dashboardService.getRecentActivity(20), dashboardService.getAgentUtilization()])`
    4. Layout:
       - Container: `container mx-auto max-w-6xl px-4 py-8`
       - h1: "Dashboard" with text-3xl font-bold mb-8
       - StatCards component with stats prop
       - Below stats: `grid grid-cols-1 lg:grid-cols-2 gap-6 mt-6`
         - Left: ActivityFeed with activities prop
         - Right: UtilizationChart with utilization data prop
    5. Remove old imports (Link, agentService, chatService) and old placeholder content
    6. Keep it as a pure server component (no "use client")
  </action>
  <verify>
    <automated>cd /Users/luke/onewave-agency && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>Dashboard page fetches from dashboardService and renders all three sections. Full test suite passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete dashboard</name>
  <action>
    Human verifies the complete dashboard visually.

    What was built: Complete dashboard with stat cards, activity feed, and agent utilization chart replacing the old placeholder page.

    How to verify:
    1. Run `npm run dev` and visit http://localhost:3000
    2. Verify stat cards show: Active Sessions count, Agents Used count, Tokens Consumed (formatted)
    3. Verify activity feed shows chronological list of recent chats, missions, and deliverable actions
    4. Verify utilization chart shows horizontal bars with agent names and colors
    5. If no data exists yet: verify empty states show meaningful messages (not broken/blank)
    6. Check dark mode renders correctly (icons, chart, borders)
  </action>
  <verify>Human approves visual output</verify>
  <done>User confirms dashboard looks correct and all three sections render properly</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- `npx vitest run` -- full test suite passes
- Dashboard loads at / with all three sections visible
- Empty states render when no data exists
</verification>

<success_criteria>
- Dashboard shows 3 stat cards with real data from Prisma aggregation (DASH-01)
- Activity feed displays merged, sorted timeline of chats, missions, and deliverables (DASH-02)
- Recharts bar chart renders agent utilization with per-agent colors (DASH-03)
- All existing tests continue to pass
- Human verification confirms visual correctness
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-and-polish/05-02-SUMMARY.md`
</output>
