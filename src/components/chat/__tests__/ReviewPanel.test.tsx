import { describe, test } from "vitest";

describe("ReviewPanel", () => {
  test.todo(
    "renders approve and revise buttons for pending deliverable"
  );

  test.todo("shows approved badge after approve click");

  test.todo("shows revision textarea when revise button clicked");

  test.todo(
    "calls onRequestRevision with feedback text on submit"
  );

  test.todo("disables actions when already approved");
});
