---
phase: 02-chat-and-streaming
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/lib/services/__tests__/chat.test.ts
  - src/app/api/chat/__tests__/route.test.ts
  - src/components/chat/__tests__/MessageBubble.test.tsx
  - src/test/mocks/anthropic.ts
autonomous: true
requirements: [CHAT-01, CHAT-02, CHAT-03, CHAT-04]

must_haves:
  truths:
    - "Test stubs exist for all CHAT requirements before implementation begins"
    - "Anthropic SDK mock utilities enable isolated testing of streaming route"
    - "Running npm test produces failing tests that will pass once implementation lands"
  artifacts:
    - path: "src/lib/services/__tests__/chat.test.ts"
      provides: "Unit test stubs for chatService CRUD (CHAT-01, CHAT-04)"
      min_lines: 30
    - path: "src/app/api/chat/__tests__/route.test.ts"
      provides: "Integration test stubs for SSE streaming route (CHAT-02)"
      min_lines: 30
    - path: "src/components/chat/__tests__/MessageBubble.test.tsx"
      provides: "Unit test stubs for markdown rendering (CHAT-03)"
      min_lines: 20
    - path: "src/test/mocks/anthropic.ts"
      provides: "Mock Anthropic SDK for test isolation"
      exports: ["createMockAnthropicClient", "mockStream"]
  key_links:
    - from: "src/app/api/chat/__tests__/route.test.ts"
      to: "src/test/mocks/anthropic.ts"
      via: "vi.mock import"
      pattern: "vi\\.mock.*anthropic"
---

<objective>
Create test stubs and mock utilities for all Phase 2 CHAT requirements before any implementation begins.

Purpose: Satisfy Nyquist sampling — every implementation task has a corresponding vitest-based automated verification command. These stubs define expected behavior (RED phase) so implementation tasks can target GREEN.

Output: Three test files with failing stubs and one Anthropic SDK mock utility module.
</objective>

<execution_context>
@/Users/luke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-chat-and-streaming/02-RESEARCH.md
@.planning/phases/02-chat-and-streaming/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Anthropic SDK mock and chat service test stubs</name>
  <files>src/test/mocks/anthropic.ts, src/lib/services/__tests__/chat.test.ts</files>
  <action>
1. **Create src/test/mocks/anthropic.ts**: Mock Anthropic SDK utilities for test isolation:
   - `createMockAnthropicClient()` — returns a mock object matching the shape of `new Anthropic()` with `messages.stream()` mocked
   - `mockStream(textChunks: string[])` — returns a mock stream object that:
     - Has `.on("text", cb)` that calls cb with each chunk
     - Has `.on("end", cb)` that calls cb after all text chunks
     - Has `.on("error", cb)` for error simulation
     - Has `.currentMessage` returning `{ usage: { input_tokens: 10, output_tokens: 20 } }`
   - Export a helper `mockAnthropicModule()` that returns the mock for use with `vi.mock("@anthropic-ai/sdk")`

2. **Create src/lib/services/__tests__/chat.test.ts**: Test stubs for chatService covering CHAT-01 and CHAT-04:
   - Mock `@/lib/prisma` using `vi.mock`
   - Test suite "chatService":
     - `describe("createSession")`:
       - `it("creates a session with agentId and default model")` — verify prisma.chatSession.create called with correct args
       - `it("creates a session with specified model for CHAT-04")` — verify model parameter passed through
     - `describe("getSession")`:
       - `it("returns session with agent and messages included")` — verify findUnique includes agent+messages
     - `describe("addMessage")`:
       - `it("creates a message with role and content")` — verify prisma.message.create
       - `it("stores token counts when provided")` — verify inputTokens/outputTokens
     - `describe("getSessionMessages")`:
       - `it("returns messages ordered by createdAt asc")` — verify orderBy
   - All tests should be `it.todo(...)` OR have assertions against mocked prisma calls that will fail until implementation exists
   - Use `beforeEach` to reset mocks
  </action>
  <verify>
    <automated>cd /Users/luke/onewave-agency && npx vitest run src/lib/services/__tests__/chat.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>chat.test.ts exists with test stubs for all chatService methods. Anthropic mock utility exports createMockAnthropicClient and mockStream. Tests either fail (RED) or are marked todo.</done>
</task>

<task type="auto">
  <name>Task 2: Route and MessageBubble test stubs</name>
  <files>src/app/api/chat/__tests__/route.test.ts, src/components/chat/__tests__/MessageBubble.test.tsx</files>
  <action>
1. **Create src/app/api/chat/__tests__/route.test.ts**: Integration test stubs for SSE streaming route (CHAT-02):
   - Mock `@anthropic-ai/sdk` using `vi.mock` and the mock utilities from `src/test/mocks/anthropic.ts`
   - Mock `@/lib/services/settings` to return a fake API key
   - Mock `@/lib/services/agent` to return a fake agent with systemPrompt
   - Mock `@/lib/services/chat` for message persistence
   - Test suite "POST /api/chat":
     - `it("returns 401 when API key is not configured")` — mock getApiKey returning null, verify 401 response
     - `it("returns 404 when agent is not found")` — mock getBySlug returning null, verify 404
     - `it("streams SSE text events from Claude API")` — use mockStream with text chunks, read response body, verify SSE `data:` lines contain type:"text" events
     - `it("sends done event with usage after stream completes")` — verify final SSE event has type:"done" with usage object
     - `it("sends error event on stream failure")` — configure mock stream to emit error, verify type:"error" event
   - Import the route's POST handler directly: `import { POST } from "../route"` (adjust path as needed)
   - Create Request objects: `new Request("http://localhost/api/chat", { method: "POST", body: JSON.stringify({...}) })`

2. **Create src/components/chat/__tests__/MessageBubble.test.tsx**: Unit test stubs for markdown rendering (CHAT-03):
   - Use `@testing-library/react` for rendering (`render`, `screen`)
   - Test suite "MessageBubble":
     - `it("renders user messages as plain text")` — render with role="user", verify content visible, no markdown processing
     - `it("renders assistant messages with markdown formatting")` — render with role="assistant" and markdown content (e.g., `**bold**`), verify rendered as `<strong>`
     - `it("renders code blocks with syntax highlighting")` — render with content containing a fenced code block, verify `hljs` class applied (rehype-highlight adds hljs classes)
     - `it("renders tables from GFM markdown")` — render with markdown table, verify `<table>` element exists
   - All tests should assert expected DOM output — they will fail until MessageBubble component is implemented
  </action>
  <verify>
    <automated>cd /Users/luke/onewave-agency && npx vitest run src/app/api/chat/__tests__/route.test.ts src/components/chat/__tests__/MessageBubble.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <done>route.test.ts has 5 test stubs covering SSE streaming, auth, and error handling. MessageBubble.test.tsx has 4 test stubs covering markdown rendering and syntax highlighting. All tests fail (RED) or are todo.</done>
</task>

</tasks>

<verification>
- `npx vitest run` shows all new test files discovered
- Tests are either failing (RED) or marked as todo
- No watch-mode flags used
- Mock utilities importable from src/test/mocks/anthropic.ts
</verification>

<success_criteria>
1. Three test files exist at the paths specified in VALIDATION.md Wave 0 Requirements
2. Anthropic SDK mock utility exists and exports helper functions
3. Test stubs cover all four CHAT requirements (CHAT-01 through CHAT-04)
4. Running `npx vitest run` completes in under 5 seconds
</success_criteria>

<output>
After completion, create `.planning/phases/02-chat-and-streaming/02-00-SUMMARY.md`
</output>
