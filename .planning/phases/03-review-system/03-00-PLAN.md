---
phase: 03-review-system
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/types/chat.ts
  - src/lib/deliverable-parser.ts
  - src/lib/__tests__/deliverable-parser.test.ts
  - src/components/chat/__tests__/ReviewPanel.test.tsx
  - src/store/__tests__/chat-review.test.ts
autonomous: true
requirements:
  - REVW-01
  - REVW-02
  - REVW-03
  - REVW-04

must_haves:
  truths:
    - "Deliverable parser correctly extracts <deliverable> blocks from message content"
    - "Test stubs exist for all REVW requirements before implementation begins"
    - "Prisma schema includes Deliverable model with status tracking"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Deliverable model with messageId, index, status, feedback"
      contains: "model Deliverable"
    - path: "src/lib/deliverable-parser.ts"
      provides: "parseDeliverables function"
      exports: ["parseDeliverables", "ParsedContent"]
    - path: "src/types/chat.ts"
      provides: "DeliverableStatus type, DeliverableState interface"
      exports: ["DeliverableStatus", "DeliverableState"]
    - path: "src/lib/__tests__/deliverable-parser.test.ts"
      provides: "Parser unit tests"
      min_lines: 30
    - path: "src/components/chat/__tests__/ReviewPanel.test.tsx"
      provides: "ReviewPanel component test stubs"
      min_lines: 20
    - path: "src/store/__tests__/chat-review.test.ts"
      provides: "Chat store review extension test stubs"
      min_lines: 20
  key_links:
    - from: "prisma/schema.prisma"
      to: "Message model"
      via: "Deliverable.messageId relation"
      pattern: "message.*Message.*@relation"
---

<objective>
Wave 0: Foundation for the review system -- Prisma schema, types, deliverable parser, and test stubs.

Purpose: Establish the data layer, type contracts, and test scaffolds that all subsequent review plans build against. The parser is implemented here (not just stubbed) because it's pure logic with clear TDD behavior.
Output: Deliverable model in Prisma, extended chat types, working parser with tests, test stubs for UI and store.
</objective>

<execution_context>
@/Users/luke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-review-system/03-RESEARCH.md
@.planning/phases/03-review-system/03-VALIDATION.md

<interfaces>
<!-- Existing types the executor needs -->

From src/types/chat.ts:
```typescript
export type ChatSession = PrismaChatSession;
export type Message = PrismaMessage;
export interface ChatMessage {
  role: "user" | "assistant";
  content: string;
}
export type SSEEvent =
  | { type: "text"; text: string }
  | { type: "done"; usage: { input_tokens: number; output_tokens: number } }
  | { type: "error"; message: string };
```

From prisma/schema.prisma (Message model):
```prisma
model Message {
  id           String      @id @default(cuid())
  sessionId    String
  session      ChatSession @relation(fields: [sessionId], references: [id], onDelete: Cascade)
  role         String
  content      String
  inputTokens  Int?
  outputTokens Int?
  createdAt    DateTime    @default(now())
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prisma Deliverable model, types, and deliverable parser with tests</name>
  <files>prisma/schema.prisma, src/types/chat.ts, src/lib/deliverable-parser.ts, src/lib/__tests__/deliverable-parser.test.ts</files>
  <behavior>
    - parseDeliverables("no deliverable here") returns { segments: [{ type: 'text', content: 'no deliverable here' }], hasDeliverables: false }
    - parseDeliverables("before &lt;deliverable&gt;content&lt;/deliverable&gt; after") returns 3 segments (text, deliverable, text) with hasDeliverables: true
    - parseDeliverables with multiple deliverable blocks returns correct indices (0, 1)
    - parseDeliverables("&lt;deliverable&gt;only&lt;/deliverable&gt;") works with no surrounding text
    - parseDeliverables("") returns single empty text segment
  </behavior>
  <action>
1. Add Deliverable model to prisma/schema.prisma:
   - Fields: id (cuid), messageId (String), index (Int), status (String, default "pending"), feedback (String?), createdAt, updatedAt
   - Relation: message Message @relation(fields: [messageId], references: [id], onDelete: Cascade)
   - Unique constraint: @@unique([messageId, index])
   - Add `deliverables Deliverable[]` to existing Message model

2. Run `npx prisma db push` to apply schema changes.

3. Add to src/types/chat.ts:
   - `export type DeliverableStatus = "pending" | "approved" | "revised";`
   - `export interface DeliverableState { status: DeliverableStatus; feedback?: string; }`
   - `export interface ParsedSegment` — text or deliverable segment type
   - `export interface ParsedContent { segments: ParsedSegment[]; hasDeliverables: boolean; }`

4. Create src/lib/deliverable-parser.ts implementing `parseDeliverables(content: string): ParsedContent` using regex `/<deliverable>([\s\S]*?)<\/deliverable>/g`. Follow the exact pattern from RESEARCH.md Pattern 1.

5. Write tests in src/lib/__tests__/deliverable-parser.test.ts covering all behaviors listed above. Tests first (RED), then implement parser (GREEN).
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/deliverable-parser.test.ts</automated>
  </verify>
  <done>Deliverable model exists in schema, types exported from chat.ts, parser passes all tests with correct segment extraction</done>
</task>

<task type="auto">
  <name>Task 2: Test stubs for ReviewPanel component and chat store review extensions</name>
  <files>src/components/chat/__tests__/ReviewPanel.test.tsx, src/store/__tests__/chat-review.test.ts</files>
  <action>
1. Create src/components/chat/__tests__/ReviewPanel.test.tsx with test stubs (using `test.todo`):
   - "renders approve and revise buttons for pending deliverable"
   - "shows approved badge after approve click"
   - "shows revision textarea when revise button clicked"
   - "calls onRequestRevision with feedback text on submit"
   - "disables actions when already approved"

2. Create src/store/__tests__/chat-review.test.ts with test stubs (using `test.todo`):
   - "approveDeliverable updates deliverable state to approved"
   - "requestRevision updates state and calls sendMessage with revision prompt"
   - "deliverable state persists across re-renders"

These are stubs only (test.todo) -- they will be implemented alongside their production code in Plans 01 and 02.
  </action>
  <verify>
    <automated>npx vitest run src/components/chat/__tests__/ReviewPanel.test.tsx src/store/__tests__/chat-review.test.ts</automated>
  </verify>
  <done>Test stub files exist with todo tests for all REVW requirements, vitest finds and skips them without errors</done>
</task>

</tasks>

<verification>
- `npx prisma db push` succeeds without errors
- `npx vitest run src/lib/__tests__/deliverable-parser.test.ts` -- all parser tests pass
- `npx vitest run` -- full suite passes (existing tests + new stubs)
- Deliverable model visible in generated Prisma client
</verification>

<success_criteria>
- Prisma schema has Deliverable model with messageId/index unique constraint
- DeliverableStatus and ParsedContent types exported from src/types/chat.ts
- parseDeliverables function correctly splits content into text and deliverable segments
- Test stubs exist for ReviewPanel and chat store review extensions
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/03-review-system/03-00-SUMMARY.md`
</output>
