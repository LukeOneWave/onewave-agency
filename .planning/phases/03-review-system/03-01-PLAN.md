---
phase: 03-review-system
plan: 01
type: execute
wave: 1
depends_on:
  - "03-00"
files_modified:
  - src/components/chat/ReviewPanel.tsx
  - src/lib/services/deliverable.ts
  - src/app/api/deliverables/[id]/route.ts
  - src/app/api/chat/route.ts
  - src/components/chat/__tests__/ReviewPanel.test.tsx
autonomous: true
requirements:
  - REVW-01
  - REVW-02
  - REVW-03

must_haves:
  truths:
    - "ReviewPanel component renders approve and revise buttons for a pending deliverable"
    - "User can approve a deliverable via API and see status change"
    - "User can enter revision feedback in a textarea"
    - "System prompt instructs agents to wrap deliverables in <deliverable> tags"
  artifacts:
    - path: "src/components/chat/ReviewPanel.tsx"
      provides: "Inline review panel with approve/revise actions"
      exports: ["ReviewPanel"]
      min_lines: 40
    - path: "src/lib/services/deliverable.ts"
      provides: "Deliverable CRUD service"
      exports: ["deliverableService"]
    - path: "src/app/api/deliverables/[id]/route.ts"
      provides: "PATCH endpoint for deliverable status updates"
      exports: ["PATCH"]
    - path: "src/app/api/chat/route.ts"
      provides: "System prompt with deliverable instruction appended"
  key_links:
    - from: "src/components/chat/ReviewPanel.tsx"
      to: "/api/deliverables/[id]"
      via: "fetch PATCH call"
      pattern: "fetch.*api/deliverables"
    - from: "src/app/api/chat/route.ts"
      to: "agent.systemPrompt"
      via: "deliverable instruction appended"
      pattern: "deliverableInstruction|<deliverable>"
---

<objective>
Build the ReviewPanel UI component, deliverable service layer, API route, and system prompt amendment.

Purpose: Create all the building blocks for the review workflow -- the visual panel, the persistence layer, and the agent instruction that triggers deliverable output.
Output: ReviewPanel component, deliverable service, PATCH API endpoint, amended system prompt.
</objective>

<execution_context>
@/Users/luke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-review-system/03-RESEARCH.md
@.planning/phases/03-review-system/03-00-SUMMARY.md

<interfaces>
<!-- Types created by Plan 00 -->
From src/types/chat.ts (after Plan 00):
```typescript
export type DeliverableStatus = "pending" | "approved" | "revised";
export interface DeliverableState { status: DeliverableStatus; feedback?: string; }
export interface ParsedContent { segments: ParsedSegment[]; hasDeliverables: boolean; }
```

From prisma/schema.prisma (after Plan 00):
```prisma
model Deliverable {
  id           String   @id @default(cuid())
  messageId    String
  message      Message  @relation(fields: [messageId], references: [id], onDelete: Cascade)
  index        Int
  status       String   @default("pending")
  feedback     String?
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
  @@unique([messageId, index])
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deliverable service, API route, and system prompt amendment</name>
  <files>src/lib/services/deliverable.ts, src/app/api/deliverables/[id]/route.ts, src/app/api/chat/route.ts</files>
  <action>
1. Create src/lib/services/deliverable.ts:
   - `getByMessageId(messageId: string)` -- returns all deliverables for a message
   - `getBySessionId(sessionId: string)` -- returns all deliverables for a session (via message relation)
   - `upsertStatus(messageId: string, index: number, status: string, feedback?: string)` -- upsert using the @@unique([messageId, index]) constraint. Use prisma.deliverable.upsert with where: { messageId_index: { messageId, index } }
   - Import prisma from "@/lib/prisma"

2. Create src/app/api/deliverables/[id]/route.ts:
   - The `[id]` param is the messageId
   - PATCH handler: accepts JSON body `{ index: number, status: string, feedback?: string }`
   - Validates status is one of "pending", "approved", "revised"
   - Calls deliverableService.upsertStatus (lazy creation -- creates Deliverable record on first interaction)
   - Returns updated deliverable as JSON
   - GET handler: returns all deliverables for the messageId

3. Amend src/app/api/chat/route.ts:
   - Add a `deliverableInstruction` constant (exact text from RESEARCH.md Pattern 2)
   - Append it to `agent.systemPrompt` before passing to `client.messages.stream()` on line 74
   - Change line 74 from `system: agent.systemPrompt` to `system: agent.systemPrompt + deliverableInstruction`
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <done>Deliverable service provides upsert/query operations, API route handles PATCH for status updates with lazy creation, chat route appends deliverable instruction to all agent system prompts</done>
</task>

<task type="auto">
  <name>Task 2: ReviewPanel component with approve/revise UI and implement test stubs</name>
  <files>src/components/chat/ReviewPanel.tsx, src/components/chat/__tests__/ReviewPanel.test.tsx</files>
  <action>
1. Create src/components/chat/ReviewPanel.tsx:
   - Props: `{ messageId: string; deliverableIndex: number; status: DeliverableStatus; onApprove: () => void; onRequestRevision: (feedback: string) => void; }`
   - Renders a horizontal bar below deliverable content with:
     - Status badge using shadcn Badge: "Pending" (default/secondary), "Approved" (green variant), "Revision Requested" (yellow variant)
     - Approve button: shadcn Button with Check icon from lucide-react, text "Approve". Disabled if status !== "pending". onClick calls onApprove.
     - Revise button: shadcn Button variant="outline" with RotateCcw icon, text "Request Revision". Disabled if status !== "pending". onClick toggles a revision textarea.
   - When revise clicked, expand a shadcn Textarea below the buttons for feedback notes
   - Submit button below textarea: "Send Feedback". onClick calls onRequestRevision(feedbackText) and clears textarea.
   - Use "use client" directive
   - Tailwind styling: border-t border-border/50 pt-3 mt-3 for separator feel

2. Implement the test stubs in src/components/chat/__tests__/ReviewPanel.test.tsx (replace test.todo with real tests):
   - Import ReviewPanel, render with @testing-library/react
   - Test: renders approve and revise buttons for pending status
   - Test: approve button calls onApprove
   - Test: clicking revise shows textarea
   - Test: submitting feedback calls onRequestRevision with text
   - Test: buttons disabled when status is "approved"
  </action>
  <verify>
    <automated>npx vitest run src/components/chat/__tests__/ReviewPanel.test.tsx</automated>
  </verify>
  <done>ReviewPanel renders approve/revise buttons with correct enable/disable states, feedback textarea expands on revise click, all ReviewPanel tests pass</done>
</task>

</tasks>

<verification>
- `npx vitest run` -- full suite green
- `npx tsc --noEmit` -- no type errors
- ReviewPanel test file has real tests (not todos) covering REVW-01, REVW-02, REVW-03
</verification>

<success_criteria>
- ReviewPanel component renders with approve/revise buttons and status badge
- Deliverable API route handles PATCH for status updates with lazy DB creation
- Chat API route appends deliverable instruction to agent system prompts
- All ReviewPanel tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-review-system/03-01-SUMMARY.md`
</output>
