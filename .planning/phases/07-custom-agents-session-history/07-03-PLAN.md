---
phase: 07-custom-agents-session-history
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - src/app/chat/page.tsx
  - src/lib/services/chat.ts
  - src/lib/services/__tests__/chat.test.ts
autonomous: false
requirements:
  - UX-03

must_haves:
  truths:
    - "User can see a list of past chat sessions sorted by most recent"
    - "Each session shows the agent name, division badge, session title or first message preview, message count, and date"
    - "User can click any session to navigate to /chat/[sessionId] and resume the conversation"
    - "Sessions with custom agents show the custom agent's name correctly"
    - "Empty state guides user to browse agents to start a chat"
  artifacts:
    - path: "src/app/chat/page.tsx"
      provides: "Enhanced session history list page"
      min_lines: 40
    - path: "src/lib/services/chat.ts"
      provides: "Enhanced getRecentSessions with first message preview and agent color"
  key_links:
    - from: "src/app/chat/page.tsx"
      to: "src/lib/services/chat.ts"
      via: "chatService.getRecentSessions()"
      pattern: "chatService\\.getRecentSessions"
    - from: "src/app/chat/page.tsx"
      to: "/chat/[sessionId]"
      via: "Link component for session resumption"
      pattern: "href.*chat.*session"
---

<objective>
Enhance the session history page so users can browse past conversations with richer context (agent info, session title, first message preview) and resume any session.

Purpose: Users need to revisit past conversations. The current /chat page shows basic session info -- this plan adds agent color, session title, first message preview, and visual polish to make session browsing useful.

Output: Enhanced /chat page with rich session cards showing agent context and conversation preview.
</objective>

<execution_context>
@/Users/luke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-custom-agents-session-history/07-RESEARCH.md

@src/app/chat/page.tsx
@src/lib/services/chat.ts
@src/components/agents/AgentCard.tsx

<interfaces>
<!-- Existing chat service and page patterns -->

From src/lib/services/chat.ts:
```typescript
export const chatService = {
  async getRecentSessions(limit = 20) {
    return prisma.chatSession.findMany({
      orderBy: { updatedAt: "desc" },
      take: limit,
      include: {
        agent: { select: { name: true, division: true, slug: true } },
        _count: { select: { messages: true } },
      },
    });
  },
  async updateSessionTitle(id: string, title: string) { ... },
};
```

From src/app/chat/page.tsx:
- Server component rendering session list
- Each session links to /chat/[sessionId]
- Shows agent name, message count, date
- Empty state with link to /agents

Prisma ChatSession model: id, agentId, agent (Agent relation), model, title?, createdAt, updatedAt, messages Message[]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enhance chat service and session history page</name>
  <files>src/lib/services/chat.ts, src/app/chat/page.tsx, src/lib/services/__tests__/chat.test.ts</files>
  <behavior>
    - getRecentSessions returns sessions with agent name, division, slug, color, isCustom, message count, and first user message content (truncated to 100 chars)
    - Sessions are ordered by updatedAt descending
    - Sessions with no messages still appear (empty preview)
  </behavior>
  <action>
1. Update `chatService.getRecentSessions()` in `src/lib/services/chat.ts`:
   - Add `color: true` and `isCustom: true` to the agent select
   - Add first message include: `messages: { take: 1, orderBy: { createdAt: "asc" }, where: { role: "user" }, select: { content: true } }`
   - This gives us the first user message as a preview

2. Add tests to `src/lib/services/__tests__/chat.test.ts`:
   - Test that getRecentSessions returns agent color and isCustom
   - Test ordering by updatedAt desc

3. Rewrite `src/app/chat/page.tsx` with enhanced session cards:
   - Page title: "Chat History" (more descriptive than just "Chat")
   - Subtitle: "{count} conversations" with muted foreground
   - Each session card (still a Link to `/chat/${session.id}`):
     - Left side: colored dot using `agent.color` (8px circle), agent name (font-medium), division badge (small, secondary variant)
     - Below agent info: session title if exists, OR first message preview truncated to 100 chars with "..." suffix, OR "No messages yet" in muted text
     - Right side: message count (e.g., "12 msgs"), relative date (use simple logic: "Today", "Yesterday", or date string)
   - Card styling: rounded-2xl bg-card p-4 shadow-sm hover:shadow-md transition-all hover:-translate-y-0.5 (matches existing pattern from current page)
   - Keep existing empty state but update heading to "No conversations yet"
   - Show "Custom" badge next to agent name if `agent.isCustom === true`
  </action>
  <verify>
    <automated>cd /Users/luke/onewave-agency && npx vitest run src/lib/services/__tests__/chat.test.ts</automated>
  </verify>
  <done>Session history page shows rich session cards with agent color, name, division, session preview, message count, and date. Clicking any session navigates to resume it. Custom agent sessions show custom badge.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Phase 7 -- agent CRUD and session history</name>
  <action>
Human verification of the full Phase 7 feature set across all 3 plans. Run dev server with `npm run dev` before testing.

What was built: Custom agent builder (create/edit/clone/delete) and enhanced session history. Users can create custom agents via /agents/new, edit/clone/delete from agent detail pages, browse past sessions on /chat with agent context and message previews.

How to verify:
1. Navigate to http://localhost:3000/agents -- verify "Create Agent" button in header
2. Click "Create Agent" -- verify form has name, division, description, role, personality, process, color fields
3. Fill out form and submit -- verify redirect to new agent's detail page with "Custom" badge
4. On the new custom agent's detail page -- verify Edit, Clone, Delete buttons visible
5. Click Edit -- verify form pre-filled with agent data, make a change, save
6. Navigate to any seeded agent -- verify only Clone button visible (no Edit/Delete)
7. Click Clone on a seeded agent -- verify create form pre-filled with "(Copy)" name suffix
8. Navigate to http://localhost:3000/chat -- verify enhanced session list with agent names, previews, dates
9. Click a session -- verify it navigates to the chat and you can continue the conversation
10. Return to custom agent, click Delete, confirm -- verify agent removed and redirected to catalog
  </action>
  <verify>
    <automated>cd /Users/luke/onewave-agency && npx vitest run && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>User confirms all 10 verification steps pass. Type "approved" or describe issues to fix.</done>
</task>

</tasks>

<verification>
- `npx vitest run` -- all tests pass
- `npx next build` -- build succeeds
- Manual: full agent CRUD flow works end-to-end
- Manual: session history shows enriched data
</verification>

<success_criteria>
- Session history shows agent color, name, division, preview text, message count, and date
- Clicking a session resumes the conversation at /chat/[sessionId]
- All 5 phase success criteria from ROADMAP are met
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/07-custom-agents-session-history/07-03-SUMMARY.md`
</output>
