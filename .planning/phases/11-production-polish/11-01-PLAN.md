---
phase: 11-production-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/PageTransition.tsx
  - src/components/layout/AppShell.tsx
  - src/components/layout/__tests__/PageTransition.test.tsx
  - src/components/projects/ProjectCard.tsx
  - src/components/projects/TaskCard.tsx
  - src/components/layout/Sidebar.tsx
autonomous: true
requirements:
  - UX-05

must_haves:
  truths:
    - "Page content fades in on every route navigation"
    - "All clickable cards and interactive elements have consistent hover/press feedback"
  artifacts:
    - path: "src/components/layout/PageTransition.tsx"
      provides: "Layout-level page transition wrapper"
      contains: "animate-in fade-in"
    - path: "src/components/layout/__tests__/PageTransition.test.tsx"
      provides: "Unit test for PageTransition rendering and className"
    - path: "src/components/layout/AppShell.tsx"
      provides: "PageTransition wrapper around children"
      contains: "PageTransition"
  key_links:
    - from: "src/components/layout/AppShell.tsx"
      to: "src/components/layout/PageTransition.tsx"
      via: "import and wrap children"
      pattern: "<PageTransition>"
    - from: "src/components/layout/PageTransition.tsx"
      to: "usePathname"
      via: "key={pathname} to force remount on navigation"
      pattern: "key=.pathname."
---

<objective>
Create the PageTransition wrapper component and standardize hover/press feedback across all interactive elements.

Purpose: Establishes the page-level transition animation that fires on every route change, and ensures all clickable cards have consistent hover lift + active press feedback matching the AgentCard baseline.
Output: PageTransition.tsx component, updated AppShell integration, standardized hover/press classes on ProjectCard and TaskCard, unit test.
</objective>

<execution_context>
@/Users/luke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-production-polish/11-RESEARCH.md

<interfaces>
<!-- Key layout components the executor needs -->

From src/components/layout/AppShell.tsx:
```tsx
"use client";
export function AppShell({ children }: { children: React.ReactNode }) {
  // ...
  return (
    <div className="flex h-screen overflow-hidden">
      <Sidebar />
      <div className="flex flex-1 flex-col overflow-hidden">
        <Header />
        <main className="flex-1 overflow-y-auto">
          <div className="mx-auto max-w-7xl p-6">{children}</div>
        </main>
      </div>
      <CommandPalette open={paletteOpen} onOpenChange={setPaletteOpen} />
    </div>
  );
}
```

From src/components/agents/AgentCard.tsx (baseline hover pattern):
```tsx
<Card className="h-full rounded-2xl shadow-sm border-0 bg-card transition-all hover:shadow-md hover:-translate-y-0.5">
```

From src/components/projects/ProjectCard.tsx (needs upgrade):
```tsx
className="block rounded-xl border bg-card p-4 hover:shadow-md transition-shadow"
```

From src/components/projects/TaskCard.tsx (needs hover state):
```tsx
className="rounded-lg border bg-card p-3 select-none"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create PageTransition wrapper + unit test + integrate into AppShell</name>
  <files>src/components/layout/PageTransition.tsx, src/components/layout/__tests__/PageTransition.test.tsx, src/components/layout/AppShell.tsx</files>
  <behavior>
    - Test 1: PageTransition renders children correctly
    - Test 2: Wrapper div has animate-in, fade-in, and slide-in-from-bottom-4 classes
    - Test 3: Wrapper div has duration-300 and ease-out classes
  </behavior>
  <action>
    1. Create `src/components/layout/PageTransition.tsx` as a "use client" component:
       - Import `usePathname` from `next/navigation`
       - Return a div with `key={pathname}` (forces remount on navigation, per Pitfall 1 from research)
       - Apply classes: `animate-in fade-in slide-in-from-bottom-4 duration-300 ease-out`
       - Use `motion-safe:` prefix on animation classes for accessibility (per research anti-pattern guidance)
       - The component accepts `{ children: React.ReactNode }` props

    2. Create `src/components/layout/__tests__/PageTransition.test.tsx`:
       - Mock `next/navigation` usePathname to return "/test"
       - Test that children render
       - Test that the wrapper div contains the expected animation classes
       - Use `@testing-library/react` render + screen

    3. Update `src/components/layout/AppShell.tsx`:
       - Import `PageTransition` from `./PageTransition`
       - Wrap `{children}` inside the `<div className="mx-auto max-w-7xl p-6">` with `<PageTransition>{children}</PageTransition>`
       - The PageTransition goes INSIDE the padding div so the animation applies to content, not the container
  </action>
  <verify>
    <automated>cd /Users/luke/onewave-agency && npm run test -- --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>PageTransition component renders with animate-in classes, unit test passes, AppShell wraps children in PageTransition</done>
</task>

<task type="auto">
  <name>Task 2: Standardize hover/press feedback on all interactive cards and elements</name>
  <files>src/components/projects/ProjectCard.tsx, src/components/projects/TaskCard.tsx, src/components/layout/Sidebar.tsx</files>
  <action>
    Audit and upgrade hover/press feedback to match the AgentCard baseline pattern. The standard pattern is:
    `transition-all duration-200 hover:shadow-md hover:-translate-y-0.5 active:scale-[0.98] active:shadow-sm`

    1. **ProjectCard.tsx** (line 29): Change `className` on the outer Link from:
       `"block rounded-xl border bg-card p-4 hover:shadow-md transition-shadow"`
       to:
       `"block rounded-xl border bg-card p-4 transition-all duration-200 hover:shadow-md hover:-translate-y-0.5 active:scale-[0.98] active:shadow-sm"`
       This adds the lift effect and press feedback that AgentCard already has.

    2. **TaskCard.tsx** (line 33): Change `className` on the outer div from:
       `"rounded-lg border bg-card p-3 select-none"`
       to:
       `"rounded-lg border bg-card p-3 select-none transition-all duration-200 hover:shadow-sm hover:-translate-y-0.5 active:scale-[0.98]"`
       Use `hover:shadow-sm` (not `md`) since task cards are smaller. Keep `select-none` for drag behavior.

    3. **Sidebar.tsx**: Verify nav link items have `transition-colors` or `transition-all`. If any nav items lack a transition class, add `transition-colors duration-150`. Do NOT change the sidebar collapse transition which already works.

    Do NOT modify AgentCard.tsx (already has the baseline pattern).
    Do NOT modify buttons (button.tsx base variant already includes transition-all).
    Do NOT add `active:scale` to the chat session cards in `chat/page.tsx` -- they already have `transition-all hover:shadow-md hover:-translate-y-0.5`.
  </action>
  <verify>
    <automated>cd /Users/luke/onewave-agency && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>ProjectCard has lift+press hover effect, TaskCard has subtle hover feedback, sidebar nav items have transition-colors. Build succeeds with no errors.</done>
</task>

</tasks>

<verification>
- `npm run test` passes (PageTransition unit test green)
- `npm run build` succeeds (no type errors, no import errors)
- PageTransition component exists with animate-in classes and usePathname key
- AppShell imports and wraps children in PageTransition
- ProjectCard and TaskCard have consistent hover/press transition classes
</verification>

<success_criteria>
- PageTransition.tsx created with key={pathname}, animate-in fade-in slide-in-from-bottom-4 classes
- AppShell wraps children in PageTransition
- All card components (AgentCard, ProjectCard, TaskCard) have hover lift + press feedback
- Unit test for PageTransition passes
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/11-production-polish/11-01-SUMMARY.md`
</output>
