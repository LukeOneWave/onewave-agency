---
phase: 01-foundation-and-agent-catalog
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/agents/page.tsx
  - src/app/agents/[slug]/page.tsx
  - src/components/agents/AgentCard.tsx
  - src/components/agents/AgentGrid.tsx
  - src/components/agents/DivisionTabs.tsx
  - src/components/agents/AgentSearch.tsx
  - src/components/agents/AgentDetail.tsx
  - src/lib/services/agent.ts
  - src/app/api/agents/route.ts
  - src/types/agent.ts
  - src/lib/services/__tests__/agent.test.ts
autonomous: true
requirements: [AGNT-01, AGNT-02, AGNT-03, AGNT-04]

must_haves:
  truths:
    - "User can see all seeded agents displayed in a grid of cards at /agents"
    - "Each agent card shows name, description (truncated), and division color badge"
    - "User can click division tabs to filter agents by division"
    - "User can type in search box to filter agents by name or description"
    - "User can click an agent card to navigate to /agents/[slug] detail page"
    - "Agent detail page shows full system prompt rendered as markdown"
  artifacts:
    - path: "src/types/agent.ts"
      provides: "Agent TypeScript interfaces"
      exports: ["Agent", "AgentCardData"]
    - path: "src/lib/services/agent.ts"
      provides: "Agent service layer for CRUD and filtering"
      exports: ["agentService"]
    - path: "src/lib/services/__tests__/agent.test.ts"
      provides: "Behavioral tests for agent service (AGNT-01 through AGNT-04)"
      contains: "agentService"
    - path: "src/app/agents/page.tsx"
      provides: "Agent catalog page with grid, tabs, and search"
      min_lines: 30
    - path: "src/app/agents/[slug]/page.tsx"
      provides: "Agent detail page with full markdown rendering"
      min_lines: 20
    - path: "src/components/agents/AgentCard.tsx"
      provides: "Individual agent card component"
      min_lines: 20
  key_links:
    - from: "src/app/agents/page.tsx"
      to: "src/lib/services/agent.ts"
      via: "Server component data fetching"
      pattern: "agentService\\.(getAll|getDivisions)"
    - from: "src/app/agents/[slug]/page.tsx"
      to: "src/lib/services/agent.ts"
      via: "Server component slug lookup"
      pattern: "agentService\\.getBySlug"
    - from: "src/components/agents/AgentCard.tsx"
      to: "/agents/[slug]"
      via: "Next.js Link navigation"
      pattern: "href.*agents/"
    - from: "src/lib/services/agent.ts"
      to: "src/lib/prisma.ts"
      via: "Prisma queries"
      pattern: "prisma\\.agent\\."
---

<objective>
Build the agent catalog UI: browsable grid of all agents with division tab filtering, text search, and detail pages with full markdown rendering. Includes behavioral tests for the agent service layer.

Purpose: This is the primary Phase 1 user-facing feature. Users need to browse, filter, and explore all 61+ agents before they can chat with them in Phase 2.
Output: Working /agents page with grid, tabs, search, and /agents/[slug] detail pages, plus agent service tests.
</objective>

<execution_context>
@/Users/luke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-agent-catalog/01-RESEARCH.md
@.planning/phases/01-foundation-and-agent-catalog/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create agent types, service layer, and behavioral tests</name>
  <files>src/types/agent.ts, src/lib/services/agent.ts, src/app/api/agents/route.ts, src/lib/services/__tests__/agent.test.ts</files>
  <behavior>
    - Test: getAll returns all seeded agents (count >= 50)
    - Test: getAll with division filter returns only agents from that division
    - Test: getAll with search filter matches agents by name substring
    - Test: getAll with search filter matches agents by description substring
    - Test: getBySlug returns a single agent with full data (systemPrompt, rawMarkdown)
    - Test: getBySlug returns null for non-existent slug
    - Test: getDivisions returns all 10 divisions
    - Test: getCount returns total agent count matching getAll().length
  </behavior>
  <action>
1. Create `src/types/agent.ts`:
   - Export `Agent` interface matching the Prisma Agent model fields (id, name, slug, division, description, color, tools, systemPrompt, rawMarkdown, isCustom, createdAt, updatedAt)
   - Export `AgentCardData` type with just the fields needed for grid display: slug, name, description, division, color

2. Create `src/lib/services/agent.ts` following 01-RESEARCH.md "Pattern 3":
   - `getAll(filters?: { division?: string; search?: string })`: Prisma findMany with optional division filter and search across name+description using `contains`. Order by division asc, name asc.
   - `getBySlug(slug: string)`: Prisma findUnique by slug
   - `getDivisions()`: Prisma groupBy on division field, return string array
   - `getCount()`: Prisma count for verification
   - All functions use the prisma singleton from `@/lib/prisma`

3. Create `src/lib/services/__tests__/agent.test.ts`:
   - Import agentService from the service layer
   - Write all behavioral tests listed above
   - These tests run against the seeded SQLite database (integration tests)

4. Create `src/app/api/agents/route.ts`:
   - GET handler accepting optional `division` and `search` query params
   - Calls agentService.getAll with filters
   - Returns JSON response
   - This API route is for potential client-side fetching; the main pages use server components with direct service calls
  </action>
  <verify>
    <automated>cd /Users/luke/onewave-agency && npx vitest run src/lib/services/__tests__/agent.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Agent service returns all seeded agents from database, getDivisions returns all 10 divisions, getBySlug returns a single agent with full data, all 8 behavioral tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Build agent grid page with cards, division tabs, and search</name>
  <files>src/app/agents/page.tsx, src/components/agents/AgentCard.tsx, src/components/agents/AgentGrid.tsx, src/components/agents/DivisionTabs.tsx, src/components/agents/AgentSearch.tsx</files>
  <action>
1. Create `src/components/agents/AgentCard.tsx`:
   - Accept AgentCardData props (slug, name, description, division, color)
   - Use shadcn Card component with CardHeader and CardContent
   - Show agent name as CardTitle, description as truncated text (line-clamp-3)
   - Show division as a Badge with color mapping per division (see 01-RESEARCH.md "Agent Card Component" for divisionColors map)
   - Wrap entire card in Next.js Link to `/agents/${agent.slug}`
   - Add hover state: `hover:border-primary/50` transition

2. Create `src/components/agents/DivisionTabs.tsx`:
   - Client component ("use client")
   - Accept divisions string array and current active division
   - Use shadcn Tabs component with TabsList and TabsTrigger
   - "All" tab shows all agents (no filter)
   - Each division tab filters to that division
   - Use URL search params (`?division=engineering`) for state so it's shareable/refreshable
   - Use `useRouter` and `useSearchParams` from next/navigation
   - Format division names for display: capitalize, replace hyphens with spaces

3. Create `src/components/agents/AgentSearch.tsx`:
   - Client component ("use client")
   - shadcn Input with search icon (lucide Search)
   - Debounced input (300ms) that updates URL search param `?search=query`
   - Use `useRouter` and `useSearchParams`
   - Placeholder: "Search agents by name or description..."

4. Create `src/components/agents/AgentGrid.tsx`:
   - Accept agents array (AgentCardData[])
   - CSS Grid layout: 1 col on small, 2 cols on md, 3 cols on lg, 4 cols on xl
   - Map agents to AgentCard components
   - Show empty state if no agents match filters: "No agents found" with suggestion to clear filters

5. Create `src/app/agents/page.tsx`:
   - Server component that reads searchParams (division, search)
   - Calls agentService.getAll with filters from searchParams
   - Calls agentService.getDivisions for tab list
   - Renders page title "Agent Catalog", agent count badge
   - Renders DivisionTabs, AgentSearch, and AgentGrid
   - Layout: title + count on top, tabs + search below, grid as main content
  </action>
  <verify>
    <automated>cd /Users/luke/onewave-agency && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Visiting /agents shows a grid of all seeded agents with division color badges, division tabs filter the grid, search input filters by text, empty state shows when no results match</done>
</task>

<task type="auto">
  <name>Task 3: Build agent detail page with markdown rendering</name>
  <files>src/app/agents/[slug]/page.tsx, src/components/agents/AgentDetail.tsx</files>
  <action>
1. Create `src/components/agents/AgentDetail.tsx`:
   - Accept full Agent data as props
   - Layout sections:
     a. Header: Agent name (h1), division badge with color, description
     b. Metadata: color indicator, tools list (if present, split by comma and show as badges)
     c. System Prompt: Full markdown body rendered with react-markdown + remark-gfm
   - Use shadcn Separator between sections
   - Use shadcn Badge for division and tools
   - Markdown styling: Apply Tailwind prose classes (`prose dark:prose-invert`) for readable markdown
   - Back navigation link to /agents

2. Create `src/app/agents/[slug]/page.tsx`:
   - Server component with params.slug
   - Call agentService.getBySlug(slug)
   - If not found, call notFound() from next/navigation
   - Generate metadata (page title = agent name)
   - Render AgentDetail component
   - Optional: generateStaticParams for static generation (call agentService.getAll, map to slugs)

3. Install @tailwindcss/typography if not already present for prose classes:
   ```
   npm install @tailwindcss/typography
   ```
   Add to globals.css or Tailwind config as needed for v4.
  </action>
  <verify>
    <automated>cd /Users/luke/onewave-agency && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Clicking an agent card navigates to /agents/[slug], detail page shows agent name, division badge, description, tools (if any), and full system prompt rendered as formatted markdown. Back link returns to catalog.</done>
</task>

</tasks>

<verification>
- `/agents` page renders grid of all agents with cards showing name, description, division badge
- Division tabs filter the grid (clicking "engineering" shows only engineering agents)
- Search filters by name and description text
- Clicking a card navigates to `/agents/[slug]`
- Detail page shows full agent info including rendered system prompt markdown
- `npx vitest run src/lib/services/__tests__/agent.test.ts` passes all behavioral tests
- `npm run build` completes without errors
</verification>

<success_criteria>
- All 61+ agents visible in grid at /agents
- 10 division tabs + "All" tab work correctly
- Search filters agents by name or description in real-time
- Agent detail page renders full markdown system prompt
- Navigation between grid and detail works bidirectionally
- All agent service behavioral tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-agent-catalog/01-02-SUMMARY.md`
</output>
