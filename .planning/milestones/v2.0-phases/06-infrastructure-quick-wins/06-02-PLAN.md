---
phase: 06-infrastructure-quick-wins
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/skeleton.tsx
  - src/app/loading.tsx
  - src/app/agents/loading.tsx
  - src/app/chat/loading.tsx
  - src/app/orchestration/loading.tsx
  - src/app/agents/[slug]/loading.tsx
autonomous: true
requirements: [UX-02, UX-04]

must_haves:
  truths:
    - "User can toggle between dark and light mode via a visible UI control, and the preference persists across page reloads"
    - "Data-fetching pages display skeleton placeholders while loading instead of blank screens or spinners"
    - "Skeleton layouts match actual page layouts to prevent layout shift"
  artifacts:
    - path: "src/components/ui/skeleton.tsx"
      provides: "shadcn Skeleton component for loading placeholders"
      exports: ["Skeleton"]
    - path: "src/app/loading.tsx"
      provides: "Dashboard loading skeleton"
      min_lines: 10
    - path: "src/app/agents/loading.tsx"
      provides: "Agent catalog loading skeleton"
      min_lines: 10
    - path: "src/app/chat/loading.tsx"
      provides: "Chat list loading skeleton"
      min_lines: 10
    - path: "src/app/orchestration/loading.tsx"
      provides: "Orchestration page loading skeleton"
      min_lines: 10
    - path: "src/app/agents/[slug]/loading.tsx"
      provides: "Agent detail loading skeleton"
      min_lines: 10
  key_links:
    - from: "src/app/loading.tsx"
      to: "src/components/ui/skeleton.tsx"
      via: "import Skeleton"
      pattern: "from.*components/ui/skeleton"
    - from: "src/app/agents/loading.tsx"
      to: "src/components/ui/skeleton.tsx"
      via: "import Skeleton"
      pattern: "from.*components/ui/skeleton"
---

<objective>
Verify the existing theme toggle works correctly in both modes and add loading skeleton placeholders to all data-fetching pages using Next.js loading.tsx convention.

Purpose: Delivers two visible UX improvements -- confirmed dark/light mode support and professional loading states across all pages.
Output: Verified theme toggle, 5 loading.tsx skeleton files covering all data-fetching routes.
</objective>

<execution_context>
@/Users/luke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-infrastructure-quick-wins/06-RESEARCH.md

@src/app/page.tsx
@src/app/agents/page.tsx
@src/app/chat/page.tsx
@src/app/orchestration/page.tsx

<interfaces>
<!-- Page layouts the skeletons must mirror -->

Dashboard (src/app/page.tsx):
- container mx-auto max-w-6xl px-4 py-8
- h-9 title, h-5 subtitle with mb-8
- StatCards: grid grid-cols-1 md:grid-cols-3 gap-4 (3 cards, h-24 each)
- Content grid: grid grid-cols-1 lg:grid-cols-2 gap-6 mt-6 (2 cards, h-80 each)

Agents (src/app/agents/page.tsx):
- container mx-auto px-4 py-8
- h1 title + Badge + subtitle with mb-6
- DivisionTabs + AgentSearch row with mb-6
- AgentGrid: grid of agent cards

Chat (src/app/chat/page.tsx):
- container mx-auto max-w-4xl px-4 py-8
- h1 title with mb-6
- Session list: space-y-2 of rounded-2xl bg-card p-4 items

Orchestration (src/app/orchestration/page.tsx):
- container mx-auto px-4 py-8
- h1 title + subtitle with mb-8
- MissionCreator form area

Existing shadcn components installed:
avatar, badge, button, card, dropdown-menu, input, scroll-area, separator, sheet, tabs, textarea, tooltip
(skeleton NOT yet installed)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn skeleton and verify theme toggle</name>
  <files>src/components/ui/skeleton.tsx</files>
  <action>
  **Step 1: Install shadcn skeleton component**

  Run: `npx shadcn@latest add skeleton`

  This copies the Skeleton component source into src/components/ui/skeleton.tsx. No npm dependency is added -- it's just a thin wrapper around a div with animate-pulse.

  **Step 2: Verify theme toggle (UX-02)**

  The theme toggle is already implemented in src/components/layout/Header.tsx using next-themes. Verify by:

  1. Start dev server if not running: `npm run dev`
  2. Check that the Header.tsx component imports useTheme from next-themes and renders Sun/Moon toggle
  3. Check that ThemeProvider is configured in the layout with attribute="class"
  4. Check that globals.css has both `:root` (light) and `.dark` variable sets
  5. Run `npm run build` to confirm no build errors in either theme mode

  UX-02 is already complete per research findings. The theme toggle button is visible in the header, toggles between dark/light, and persists via next-themes localStorage. No code changes needed for UX-02 -- just verification that it builds and works.

  If any theme-related issues are found during verification (e.g., components not styling correctly in light mode), fix them. Common issues: hardcoded colors instead of CSS variables, missing dark: variant classes.
  </action>
  <verify>
    <automated>cd /Users/luke/onewave-agency && test -f src/components/ui/skeleton.tsx && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>skeleton.tsx exists in src/components/ui/, build succeeds in both theme modes, Header.tsx has working theme toggle (verified via code inspection)</done>
</task>

<task type="auto">
  <name>Task 2: Create loading.tsx skeletons for all data-fetching pages</name>
  <files>src/app/loading.tsx, src/app/agents/loading.tsx, src/app/chat/loading.tsx, src/app/orchestration/loading.tsx, src/app/agents/[slug]/loading.tsx</files>
  <action>
  Create 5 loading.tsx files using Next.js App Router convention. Each file exports a default function that renders Skeleton components matching the actual page layout dimensions. Import Skeleton from "@/components/ui/skeleton".

  **1. src/app/loading.tsx (Dashboard)**
  Match the dashboard layout:
  ```
  container mx-auto max-w-6xl px-4 py-8
  - Title skeleton: h-9 w-48
  - Subtitle skeleton: h-5 w-64 mt-1, mb-8
  - Stat cards: grid grid-cols-1 md:grid-cols-3 gap-4, 3x Skeleton h-24 rounded-xl
  - Content grid: grid grid-cols-1 lg:grid-cols-2 gap-6 mt-6, 2x Skeleton h-80 rounded-xl
  ```

  **2. src/app/agents/loading.tsx (Agent Catalog)**
  Match the agents page layout:
  ```
  container mx-auto px-4 py-8
  - Title row: h-9 w-48 + h-6 w-16 (badge), mb-6
  - Subtitle: h-5 w-72 mt-1
  - Tabs row: h-10 w-full max-w-md, mb-6
  - Agent grid: grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4, 6x Skeleton h-40 rounded-xl
  ```

  **3. src/app/chat/loading.tsx (Chat List)**
  Match the chat page layout:
  ```
  container mx-auto max-w-4xl px-4 py-8
  - Title: h-9 w-24 mb-6
  - Session list: space-y-2, 4x Skeleton h-16 rounded-2xl
  ```

  **4. src/app/orchestration/loading.tsx (Orchestration)**
  Match the orchestration page layout:
  ```
  container mx-auto px-4 py-8
  - Title: h-9 w-48 mb-2
  - Subtitle: h-5 w-80 mt-1 mb-8
  - Main content area: Skeleton h-96 rounded-xl
  ```

  **5. src/app/agents/[slug]/loading.tsx (Agent Detail)**
  Match the agent detail page layout:
  ```
  container mx-auto max-w-4xl px-4 py-8
  - Back link: h-5 w-24 mb-6
  - Agent header: flex gap-4, Skeleton h-16 w-16 rounded-full (avatar) + column of h-8 w-48 and h-5 w-64
  - Content area: mt-8, Skeleton h-64 rounded-xl
  ```

  IMPORTANT: Use the exact same container classes as the actual pages to prevent layout shift when content replaces the skeleton. Do NOT add any custom CSS animations -- the shadcn Skeleton component has animate-pulse built in.

  Each file is a simple default export function component, no "use client" needed (these are server components by default in Next.js App Router).
  </action>
  <verify>
    <automated>cd /Users/luke/onewave-agency && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>5 loading.tsx files exist (/, /agents, /chat, /orchestration, /agents/[slug]), each mirrors the actual page layout dimensions, build succeeds without errors, skeleton component renders with animate-pulse</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with no errors
2. All 5 loading.tsx files exist in correct directories
3. Each loading.tsx imports Skeleton from @/components/ui/skeleton
4. Container classes in loading.tsx match their corresponding page.tsx files
5. Theme toggle in Header works (Sun/Moon button present, toggles theme)
6. `npx vitest run` -- existing tests still pass
</verification>

<success_criteria>
- shadcn Skeleton component installed at src/components/ui/skeleton.tsx
- Theme toggle verified working (UX-02 confirmed complete)
- 5 loading.tsx files cover all data-fetching routes
- Skeleton layouts match actual page dimensions (no layout shift)
- Build succeeds, existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-infrastructure-quick-wins/06-02-SUMMARY.md`
</output>
