import { describe, test } from "vitest";

describe("chat store review extensions", () => {
  test.todo(
    "approveDeliverable updates deliverable state to approved"
  );

  test.todo(
    "requestRevision updates state and calls sendMessage with revision prompt"
  );

  test.todo("deliverable state persists across re-renders");
});
