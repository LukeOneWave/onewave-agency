---
phase: 08-project-management-task-kanban
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/project.ts
  - src/lib/validations/project.ts
  - src/lib/validations/task.ts
  - src/lib/services/project.ts
  - src/lib/services/task.ts
  - src/lib/services/__tests__/project.test.ts
  - src/lib/services/__tests__/task.test.ts
  - src/app/api/projects/route.ts
  - src/app/api/projects/[id]/route.ts
  - src/app/api/projects/[id]/tasks/route.ts
  - src/app/api/tasks/[id]/route.ts
autonomous: true
requirements:
  - PROJ-01
  - PROJ-02
  - PROJ-03
  - PROJ-04
  - PROJ-05

must_haves:
  truths:
    - "projectService.create() persists a project with name and description"
    - "projectService.getAll() returns projects with task counts"
    - "projectService.getById() returns project with tasks and assigned agents"
    - "taskService.create() creates a task with correct order value"
    - "taskService.updateStatus() updates both status and order fields"
  artifacts:
    - path: "src/types/project.ts"
      provides: "Project, Task, TaskStatus types and constants"
      exports: ["TaskStatus", "TASK_STATUSES", "COLUMN_LABELS", "ProjectWithTasks", "TaskWithAgent"]
    - path: "src/lib/services/project.ts"
      provides: "Project CRUD operations"
      exports: ["projectService"]
    - path: "src/lib/services/task.ts"
      provides: "Task CRUD and status update operations"
      exports: ["taskService"]
    - path: "src/app/api/projects/route.ts"
      provides: "GET /api/projects and POST /api/projects"
      exports: ["GET", "POST"]
    - path: "src/app/api/tasks/[id]/route.ts"
      provides: "PATCH /api/tasks/[id] for drag persistence"
      exports: ["PATCH", "DELETE"]
  key_links:
    - from: "src/app/api/projects/route.ts"
      to: "src/lib/services/project.ts"
      via: "projectService.create/getAll"
      pattern: "projectService\\.(create|getAll)"
    - from: "src/app/api/tasks/[id]/route.ts"
      to: "src/lib/services/task.ts"
      via: "taskService.updateStatus"
      pattern: "taskService\\.updateStatus"
    - from: "src/lib/services/task.ts"
      to: "prisma.task"
      via: "Prisma ORM"
      pattern: "prisma\\.task\\.(create|update|findMany)"
---

<objective>
Build the complete backend for project management: types, Zod validations, service layer, API routes, and unit tests.

Purpose: Provide a tested data layer that the UI plans (02 and 03) will consume. All API contracts are defined here so UI work can proceed with confidence.
Output: Working API endpoints for project CRUD, task CRUD, and task status/order updates, with passing unit tests.
</objective>

<execution_context>
@/Users/luke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-project-management-task-kanban/08-RESEARCH.md

@prisma/schema.prisma
@src/lib/services/agent.ts
@src/lib/validations/agent.ts
@src/app/api/agents/route.ts
@src/lib/services/__tests__/agent-crud.test.ts

<interfaces>
<!-- Existing Prisma schema models (already in DB, no migration needed) -->
From prisma/schema.prisma:
```prisma
model Project {
  id          String   @id @default(cuid())
  name        String
  description String?
  status      String   @default("active")
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  tasks       Task[]
}

model Task {
  id              String   @id @default(cuid())
  projectId       String
  project         Project  @relation(fields: [projectId], references: [id], onDelete: Cascade)
  title           String
  description     String?
  status          String   @default("todo")
  assignedAgentId String?
  assignedAgent   Agent?   @relation(fields: [assignedAgentId], references: [id])
  order           Int      @default(0)
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
}
```

<!-- Existing patterns to follow -->
From src/lib/validations/agent.ts (zod v4 import pattern):
```typescript
import { z } from "zod/v4";
```

From src/app/api/agents/route.ts (API route pattern):
```typescript
import { NextRequest, NextResponse } from "next/server";
// Zod safeParse -> 400 on failure, service call -> 201 on create
```

From src/lib/services/__tests__/agent-crud.test.ts (test mock pattern):
```typescript
vi.mock("@/lib/prisma", () => ({
  prisma: { agent: { create: vi.fn(), findUnique: vi.fn(), ... } },
}));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Types, validations, and service layer with tests</name>
  <files>
    src/types/project.ts,
    src/lib/validations/project.ts,
    src/lib/validations/task.ts,
    src/lib/services/project.ts,
    src/lib/services/task.ts,
    src/lib/services/__tests__/project.test.ts,
    src/lib/services/__tests__/task.test.ts
  </files>
  <behavior>
    - projectService.create({ name, description }) creates and returns a project
    - projectService.getAll() returns projects with task status counts
    - projectService.getById(id) returns project with tasks including assignedAgent details, ordered by status then order
    - projectService.getById(id) returns null for non-existent project
    - taskService.create({ projectId, title, description?, assignedAgentId? }) creates task with order = max(order in same column) + 1
    - taskService.create() for first task in column sets order = 0
    - taskService.updateStatus(id, { status, order }) updates both fields
    - taskService.delete(id) removes a task
  </behavior>
  <action>
    1. Create `src/types/project.ts`:
       - Export `TASK_STATUSES = ["todo", "in_progress", "review", "done"] as const`
       - Export `type TaskStatus = typeof TASK_STATUSES[number]`
       - Export `COLUMN_LABELS: Record<TaskStatus, string>` mapping to "To Do", "In Progress", "Review", "Done"
       - Export `ProjectWithTasks` type (Project with tasks including assignedAgent select: id, name, color, slug)
       - Export `TaskWithAgent` type (Task with optional assignedAgent)

    2. Create `src/lib/validations/project.ts`:
       - Import from `zod/v4` (match existing pattern)
       - `CreateProjectSchema`: name (string, min 1, max 100), description (string, optional, max 500)
       - `UpdateProjectSchema`: partial of create, refine at least one field

    3. Create `src/lib/validations/task.ts`:
       - `CreateTaskSchema`: title (string, min 1, max 200), description (string, optional, max 1000), assignedAgentId (string, optional)
       - `UpdateTaskStatusSchema`: status (enum of TASK_STATUSES), order (number, int, min 0)

    4. Create `src/lib/services/project.ts` following agentService pattern:
       - `getAll()`: findMany with include `_count: { select: { tasks: true } }` and `tasks: { select: { status: true, assignedAgent: { select: { id: true, name: true, color: true, slug: true } } } }`, orderBy createdAt desc
       - `getById(id)`: findUnique with include tasks (include assignedAgent select id/name/color/slug), orderBy [status asc, order asc]
       - `create(data)`: prisma.project.create
       - `delete(id)`: prisma.project.delete

    5. Create `src/lib/services/task.ts`:
       - `create(data)`: Query max order for tasks in same project with status "todo", set order = maxOrder + 1 (or 0 if no tasks). Use prisma.task.aggregate({ where: { projectId, status: "todo" }, _max: { order: true } })
       - `updateStatus(id, { status, order })`: prisma.task.update with status and order
       - `delete(id)`: prisma.task.delete

    6. Write tests following agent-crud.test.ts mock pattern:
       - Mock `@/lib/prisma` with prisma.project and prisma.task mocks
       - Test each behavior listed above
  </action>
  <verify>
    <automated>cd /Users/luke/onewave-agency && npm test -- --reporter=dot src/lib/services/__tests__/project.test.ts src/lib/services/__tests__/task.test.ts</automated>
  </verify>
  <done>All service tests pass. Types exported. Validations defined with zod/v4.</done>
</task>

<task type="auto">
  <name>Task 2: API routes for projects and tasks</name>
  <files>
    src/app/api/projects/route.ts,
    src/app/api/projects/[id]/route.ts,
    src/app/api/projects/[id]/tasks/route.ts,
    src/app/api/tasks/[id]/route.ts
  </files>
  <action>
    Create four API route files following the pattern in `src/app/api/agents/route.ts`:

    1. `src/app/api/projects/route.ts`:
       - GET: call projectService.getAll(), return JSON
       - POST: parse body with CreateProjectSchema.safeParse, return 400 on validation error, call projectService.create(), return 201

    2. `src/app/api/projects/[id]/route.ts`:
       - GET: call projectService.getById(params.id), return 404 if null, return JSON
       - DELETE: call projectService.delete(params.id), return 204 (no content). Wrap in try/catch for 404/500.

    3. `src/app/api/projects/[id]/tasks/route.ts`:
       - GET: return tasks for project (already included in project getById, but useful for standalone). Call prisma.task.findMany with projectId filter, include assignedAgent, orderBy [status asc, order asc]
       - POST: parse body with CreateTaskSchema.safeParse, return 400 on failure. Call taskService.create with projectId from params + validated body. Return 201.

    4. `src/app/api/tasks/[id]/route.ts`:
       - PATCH: parse body with UpdateTaskStatusSchema.safeParse, return 400 on failure. Call taskService.updateStatus(params.id, parsed.data). Return JSON.
       - DELETE: call taskService.delete(params.id). Return 204.

    All routes: import NextRequest/NextResponse, wrap in try/catch, log errors with console.error, return 500 on unexpected errors. Use error message keyword matching for status codes (match Phase 7 pattern): "not found" -> 404.
  </action>
  <verify>
    <automated>cd /Users/luke/onewave-agency && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>All four API route files compile without TypeScript errors. Routes follow existing codebase conventions (NextRequest/NextResponse, safeParse validation, service layer delegation).</done>
</task>

</tasks>

<verification>
- `npm test -- --reporter=dot src/lib/services/__tests__/project.test.ts src/lib/services/__tests__/task.test.ts` all pass
- `npx tsc --noEmit` compiles without errors
- API routes follow same pattern as existing agents routes
</verification>

<success_criteria>
- projectService and taskService fully implemented with all CRUD methods
- Unit tests cover create, read, update status, and delete operations
- API routes wired to services with Zod validation
- Types exported for UI consumption in Plans 02 and 03
</success_criteria>

<output>
After completion, create `.planning/phases/08-project-management-task-kanban/08-01-SUMMARY.md`
</output>
