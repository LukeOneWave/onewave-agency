---
phase: 07-custom-agents-session-history
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - src/components/agents/AgentForm.tsx
  - src/app/agents/new/page.tsx
  - src/app/agents/[slug]/edit/page.tsx
  - src/app/agents/[slug]/page.tsx
  - src/components/agents/AgentDetail.tsx
  - src/components/agents/AgentCard.tsx
  - src/app/agents/page.tsx
autonomous: true
requirements:
  - AGNT-01
  - AGNT-02
  - AGNT-03

must_haves:
  truths:
    - "User can navigate to /agents/new and see a form with name, division, description, role, personality, process, and color fields"
    - "User can fill out the form and submit to create a custom agent, then be redirected to the new agent's detail page"
    - "User can see edit/clone/delete buttons on custom agent detail pages but NOT on seeded agent pages"
    - "User can click edit on a custom agent and see the form pre-filled with existing data"
    - "User can click clone on any agent and see the create form pre-filled with that agent's data (name suffixed with Copy)"
    - "Custom agents show a 'Custom' badge in the agent catalog grid"
  artifacts:
    - path: "src/components/agents/AgentForm.tsx"
      provides: "Shared create/edit form component for agents"
      min_lines: 80
    - path: "src/app/agents/new/page.tsx"
      provides: "Create agent page (server component wrapping AgentForm)"
    - path: "src/app/agents/[slug]/edit/page.tsx"
      provides: "Edit agent page with pre-filled form"
    - path: "src/components/agents/AgentDetail.tsx"
      provides: "Agent detail with edit/clone/delete actions for custom agents"
  key_links:
    - from: "src/components/agents/AgentForm.tsx"
      to: "/api/agents"
      via: "fetch POST for create, fetch PATCH for edit"
      pattern: "fetch.*api/agents"
    - from: "src/app/agents/new/page.tsx"
      to: "src/components/agents/AgentForm.tsx"
      via: "renders AgentForm with mode=create"
      pattern: "AgentForm.*mode.*create"
    - from: "src/components/agents/AgentDetail.tsx"
      to: "/agents/new?cloneFrom="
      via: "clone button navigates with query param"
      pattern: "cloneFrom"
---

<objective>
Build the complete agent builder UI: shared create/edit form component, create page, edit page, clone flow via query param, and custom agent actions (edit/clone/delete) on the agent detail page.

Purpose: Users can create, edit, clone, and view custom agents through the UI, consuming the API endpoints built in Plan 01.

Output: Working agent builder form, create/edit pages, clone flow, delete confirmation, and custom agent visual indicators.
</objective>

<execution_context>
@/Users/luke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-custom-agents-session-history/07-RESEARCH.md
@.planning/phases/07-custom-agents-session-history/07-01-SUMMARY.md

@src/components/agents/AgentDetail.tsx
@src/components/agents/AgentCard.tsx
@src/app/agents/page.tsx
@src/app/agents/[slug]/page.tsx
@src/types/agent.ts

<interfaces>
<!-- Types and APIs from Plan 01 that this plan consumes -->

From src/types/agent.ts (created in Plan 01):
```typescript
export interface AgentFormData {
  name: string;
  division: string;
  description: string;
  role: string;
  personality: string;
  process: string;
  color: string;
  tools?: string;
}
```

From src/lib/services/agent.ts (extended in Plan 01):
```typescript
export const agentService = {
  // existing
  async getAll(filters?: AgentFilters) { ... },
  async getBySlug(slug: string) { ... },
  async getDivisions() { ... },
  // new in Plan 01
  async create(data: AgentCreateInput) { ... },
  async update(id: string, data: AgentUpdateInput) { ... },
  async delete(id: string) { ... },
  async getForClone(slug: string) { ... },
};
```

API Endpoints (from Plan 01):
- POST /api/agents -> 201 with agent JSON
- PATCH /api/agents/[id] -> 200 with updated agent
- DELETE /api/agents/[id] -> 200 with { success: true } or 409/403

Existing component patterns:
- AgentDetail: receives `agent: Agent` prop, renders card layout with back link, name, division badge, tools, systemPrompt markdown
- AgentCard: receives `agent: AgentCardData` prop, renders Link card with name, division badge, description
- AgentDetail page: server component, uses `await params`, calls `agentService.getBySlug(slug)`, uses `generateStaticParams`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AgentForm component and create/edit pages</name>
  <files>src/components/agents/AgentForm.tsx, src/app/agents/new/page.tsx, src/app/agents/[slug]/edit/page.tsx</files>
  <action>
1. Create `src/components/agents/AgentForm.tsx` ("use client"):
   - Props: `{ mode: "create" | "edit"; agent?: Agent; divisions: string[]; cloneData?: AgentFormData }`
   - State: form fields initialized from `agent` (edit mode), `cloneData` (clone), or empty (create)
   - Fields in order:
     - Name: `<Input>` with label, placeholder "e.g., SEO Analyst"
     - Division: `<select>` dropdown populated from `divisions` prop (existing divisions only)
     - Description: `<Input>` with label, max 500 chars, placeholder "Brief description shown on agent card"
     - Role: `<Textarea>` with label, placeholder "What does this agent do?"
     - Personality: `<Textarea>` with label, placeholder "How does this agent communicate?"
     - Process: `<Textarea>` with label, placeholder "Step-by-step workflow this agent follows"
     - Color: Simple color input or 8 preset color buttons (indigo, sky, pink, violet, amber, emerald, teal, rose)
     - Tools: `<Input>` optional, placeholder "comma-separated: web-search, code-analysis"
   - Submit handler:
     - POST to `/api/agents` (create) or PATCH to `/api/agents/${agent.id}` (edit)
     - On success: `toast.success(...)`, `router.push(/agents/${saved.slug})`, `router.refresh()`
     - On error: `toast.error(...)` with server error message
     - Disable submit button while submitting, show "Creating..." / "Saving..." text
   - Cancel button: `router.back()`
   - Page title: "Create Agent" or "Edit Agent" based on mode
   - Layout: max-w-2xl centered, consistent with existing pages, rounded-2xl bg-card p-6 shadow-sm

2. Create `src/app/agents/new/page.tsx` (server component):
   - Accept `searchParams` prop to read `cloneFrom` query param
   - Fetch `divisions` via `agentService.getDivisions()`
   - If `cloneFrom` exists: call `agentService.getForClone(cloneFrom)` to get pre-fill data
   - Render `<AgentForm mode="create" divisions={divisions} cloneData={cloneData} />`
   - Metadata: title "Create Agent | OneWave"

3. Create `src/app/agents/[slug]/edit/page.tsx` (server component):
   - Await params to get slug
   - Fetch agent via `agentService.getBySlug(slug)`
   - If agent not found or `!agent.isCustom`: call `notFound()`
   - Fetch divisions via `agentService.getDivisions()`
   - Render `<AgentForm mode="edit" agent={agent} divisions={divisions} />`
   - Metadata: title "Edit {agent.name} | OneWave"

Note: The AgentForm in edit mode needs to parse the existing systemPrompt back into role/personality/process sections. Use simple splitting on "## Role", "## Personality", "## Process" headers. If parsing fails, put entire systemPrompt into the "role" field as fallback.
  </action>
  <verify>
    <automated>cd /Users/luke/onewave-agency && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>AgentForm component renders all fields, submits to correct API endpoints. Create page works at /agents/new. Edit page works at /agents/[slug]/edit and rejects non-custom agents. Clone pre-fills form via ?cloneFrom query param.</done>
</task>

<task type="auto">
  <name>Task 2: Agent detail actions, card badges, and catalog create button</name>
  <files>src/components/agents/AgentDetail.tsx, src/components/agents/AgentCard.tsx, src/app/agents/page.tsx, src/app/agents/[slug]/page.tsx, src/types/agent.ts</files>
  <action>
1. Update `src/components/agents/AgentDetail.tsx`:
   - Convert to client component ("use client") to support interactive delete
   - Add action buttons below the "Chat with Agent" button, ONLY when `agent.isCustom === true`:
     - Edit button: `<Button variant="outline">` with Pencil icon, links to `/agents/${agent.slug}/edit`
     - Clone button: `<Button variant="outline">` with Copy icon, links to `/agents/new?cloneFrom=${agent.slug}`
     - Delete button: `<Button variant="outline" className="text-destructive">` with Trash2 icon
   - Clone button should appear for ALL agents (seeded and custom) -- any agent can be cloned as starting point
   - Delete handler:
     - Show `window.confirm("Are you sure you want to delete this agent?")`
     - On confirm: `fetch(\`/api/agents/${agent.id}\`, { method: "DELETE" })`
     - On 409 (has sessions): `toast.error(data.error)` showing session count message
     - On success: `toast.success("Agent deleted")`, `router.push("/agents")`, `router.refresh()`
   - Show a "Custom" badge next to the division badge when `agent.isCustom === true`

2. Update `src/components/agents/AgentCard.tsx`:
   - Add `isCustom?: boolean` to the component props (extend AgentCardData or add separate prop)
   - When `isCustom` is true, show a small "Custom" badge (use a subtle variant) next to the division badge

3. Update `src/types/agent.ts`:
   - Add `isCustom` to `AgentCardData`: `export type AgentCardData = Pick<Agent, "slug" | "name" | "description" | "division" | "color" | "isCustom">;`

4. Update `src/app/agents/page.tsx`:
   - Add `isCustom` to the `cardData` mapping: `isCustom: a.isCustom`
   - Add a "Create Agent" button (Plus icon) in the header next to the agent count badge
   - Button links to `/agents/new`

5. Update `src/app/agents/[slug]/page.tsx`:
   - Remove `generateStaticParams` -- custom agents are dynamic, and this function would prevent them from loading. Set `export const dynamicParams = true;` (default, but explicit) and remove the static params function entirely. The page should work for both seeded and custom agent slugs.
  </action>
  <verify>
    <automated>cd /Users/luke/onewave-agency && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Custom agents show edit/clone/delete buttons on detail page. All agents show clone button. Custom agents show "Custom" badge in catalog. "Create Agent" button visible in catalog header. Agent detail page works for both seeded and custom slugs.</done>
</task>

</tasks>

<verification>
- `npx next build` succeeds without errors
- Navigate to /agents -- "Create Agent" button visible in header
- Navigate to /agents/new -- form renders with all fields
- Navigate to /agents/[seeded-slug] -- only clone button visible, no edit/delete
</verification>

<success_criteria>
- Create agent form at /agents/new with all required fields
- Edit agent form at /agents/[slug]/edit pre-fills existing data, rejects seeded agents
- Clone flow via /agents/new?cloneFrom=[slug] pre-fills from any agent
- Delete button with confirmation on custom agent detail pages only
- "Custom" badge on custom agent cards and detail page
- "Create Agent" button in agent catalog header
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/07-custom-agents-session-history/07-02-SUMMARY.md`
</output>
